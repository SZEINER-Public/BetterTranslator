# Blocked at L4

Items that could not be consumed at L1, L2 or L3. Each proposal is the smallest change to the original that would unblock it, written for a later run. Nothing outside `mac/` was changed in this run.

| Item | Why L1 fails | Why L2 fails | Why L3 fails | Proposal |
|---|---|---|---|---|
| `src/BetterTranslator.Runtime/Inference/BetterRuntime.cs` native library resolution | the assembly loads at L1 but the `Native` static ctor claims the assembly's only `DllImportResolver` slot (`BetterRuntime.cs:80-84`); a second `SetDllImportResolver` for the same assembly throws | a mirror compiles the same source, so the mirror's `Native` cctor claims the slot identically | `AssemblyLoadContext.Default.ResolvingUnmanagedDll` fires only after the primary resolver returns `IntPtr.Zero`, and `ResolveFlavor` throws `DllNotFoundException` instead (`BetterRuntime.cs:165`); a shim cannot reach past a throwing resolver | in `ResolveFlavor`, return `IntPtr.Zero` rather than throwing when no flavour loads, and let the caller surface `Finding`; or guard the `SetDllImportResolver` registration with `OperatingSystem.IsWindows()`. Either lets a host-registered `ResolvingUnmanagedDll` handler supply `libBetterRuntimeCPU.dylib`. The wrapper's `MacNativeRuntime` already subscribes that event and is inert until this lands |
| `src/BetterTranslator.Runtime/Inference/RuntimeSupport.cs` physical load path | `LoadScoped` calls `LoadLibraryExW` and `AddDllDirectory` from `kernel32.dll` (`:141-145,153-174`), which throw `DllNotFoundException` on macOS | the mirror compiles the same two `DllImport` declarations | a shim cannot substitute a method body inside a linked file, and supplying a native `kernel32` stub means building a native artifact, which is out of scope for this run | guard `LoadScoped` with `OperatingSystem.IsWindows()` and fall back to `NativeLibrary.TryLoad(path)`; make `VisualCppModules` empty off Windows so `MissingBeside` stops reporting `Incomplete` before any load is attempted (`:30-31,101-104`) |
| `src/BetterTranslator.Runtime/Inference/HostedSession.cs` out-of-process inference host | `HostPath` is `Path.Combine(AppContext.BaseDirectory, "BetterTranslator.Host.exe")` with `.exe` hardcoded (`:69-70`) | the mirror compiles the same literal | the `File.Exists` gate at `:72` is internal to the type; a shim outside it cannot change what path is tested | make the host file name platform dependent. Until then `LocalTranslator` takes its documented in-process fallback (`LocalTranslator.cs:266-305`), so the wrapper still runs; only the process isolation is lost |
| `src/BetterTranslator.App/**/*.xaml` offscreen WPF capture for the parity harness | WPF requires a Windows host and the `net10.0-windows10.0.19041.0` targeting pack; this run's host is macOS | a mirror project linking the App sources still needs `UseWPF` and the Windows targeting pack, neither of which resolves on macOS | a shim cannot supply `PresentationFramework`, `RenderTargetBitmap` or the WPF dispatcher | none needed against the original. `mac/proj/BetterTranslator.App.Capture.Mirror` is authored and linked in place, and is excluded from the macOS solution by an `$([MSBuild]::IsOSPlatform('Windows'))` condition. Run the harness on a Windows host to produce `windows.png` and the reference visual trees. Recorded in `deferred-macos.md` |
| `src/BetterTranslator.App/ViewModels/MainWindowViewModel.cs:314` `Updates` | the linked ViewModel compiles, but `public UpdatesViewModel Updates { get; } = new()` is get-only and its parameterless constructor news up `UpdaterGateway`, whose `State()` opens the Windows Service Control Manager through `advapi32` (`UpdaterGateway.cs:72`, `ServiceControl.cs:56`); `MainWindowViewModel.LoadAsync` calls `Updates.LoadAsync` (`:673`), so the shell throws `DllNotFoundException` on macOS the first time it loads | a mirror compiles the same initializer | the wrapper supplies `MacUpdaterHost`, and `SettingsViewModel.Updates` accepts it through its `init` setter, which is how the parity harness constructs Settings; but nothing outside the file can replace a get-only auto-property initializer | make `MainWindowViewModel.Updates` `{ get; init; }` like `SettingsViewModel.Updates` already is, or take an `IUpdaterHost` through a constructor parameter with the Windows gateway as the default. Until then the wrapper's `App.axaml.cs` builds the shell but the first load fails on this call, which is why running the head is listed in `deferred-macos.md` |
